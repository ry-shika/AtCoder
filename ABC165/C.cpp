#include<bits/stdc++.h>

using namespace std;

int main() {

    int N, M, Q;
    cin >> N >> M >> Q;

    int a[Q + 1], b[Q + 1], c[Q + 1], d[Q + 1];
    for(int i = 1; i <= Q; i++) {
        cin >> a[i] >> b[i] >> c[i] >> d[i];
    }

    int A[N + 1];
    


    return 0;
}